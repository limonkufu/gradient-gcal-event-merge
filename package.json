{
  "name": "gradient-gcal-event-merge",
  "version": "1.3.0",
  "private": true,
  "description": "Build and validation commands for the Gradient Merge browser extension.",
  "scripts": {
    "build": "./build.sh",
    "check": "node scripts/validate.mjs && node --check background.js && node --check events.user.js && node --check popup.js && bash -n build.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
